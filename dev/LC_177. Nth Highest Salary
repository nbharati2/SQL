#https://leetcode.com/problems/nth-highest-salary/
CREATE FUNCTION getNthHighestSalary(N INT) RETURNS INT
BEGIN
  RETURN (
      with nsal as(select  salary,dense_rank() over (order by salary desc) as rnk from Employee)
      select distinct coalesce(salary,null) from nsal where rnk = n

  );
END

